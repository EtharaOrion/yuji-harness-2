{
  "model": "claude-3-5-sonnet-latest",
  "max_tokens": 256,
  "tools": [{
    "name": "lookup",
    "description": "Look up a value by key.",
    "input_schema": {
      "type": "object",
      "properties": {"q": {"type": "string"}},
      "required": ["q"]
    }
  }],
  "tool_choice": {"type": "auto"},
  "messages": [
    {"role": "user", "content": "Use the lookup tool with q=\"x\"."}
  ]
}
